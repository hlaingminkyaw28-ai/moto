name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Only read repository contents. This is the least privilege this workflow needs.
permissions:
  contents: read

jobs:
  quality:
    name: Format and test (PHP 8.3)
    runs-on: ubuntu-latest

    steps:
      - name: Check out the code
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, xml, pdo_sqlite
          coverage: none

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/composer/files
          key: composer-${{ runner.os }}-${{ hashFiles('composer.lock') }}
          restore-keys: composer-${{ runner.os }}-

      - name: Install PHP dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Check code formatting
        run: ./vendor/bin/pint --test

      - name: Run automated tests
        run: php artisan test

      # This Laravel app has no Node/Vite build. Its deployable artifact is the
      # Docker image defined in Dockerfile, so build that actual artifact.
      - name: Build the application Docker image
        run: docker compose build app
